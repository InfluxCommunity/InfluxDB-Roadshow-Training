version: '3.6'
services:
  generators:
    image: emergency-generator:latest
    environment:
      - GENERATORS=3
      - BROKER=mosquitto
    networks:
      - mqtt
    depends_on:
      - "mosquitto"
  telegraf:
    image: telegraf:latest
    volumes:
      - ./telegraf/completed/telegraf.conf:/etc/telegraf/telegraf.conf:ro
    networks:
      - mqtt
    depends_on:
      - "mosquitto"
      - "influxdb"
  mosquitto:
    image: eclipse-mosquitto:latest
    ports:
      - 1883:1883
    networks:
      - mqtt
    volumes: 
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf
  influxdb:
    image: influxdb:latest
    environment:
      - DOCKER_INFLUXDB_INIT_MODE=setup 
      - DOCKER_INFLUXDB_INIT_USERNAME=demouser
      - DOCKER_INFLUXDB_INIT_PASSWORD=demouser
      - DOCKER_INFLUXDB_INIT_ORG=influxroadshow 
      - DOCKER_INFLUXDB_INIT_BUCKET=generators
      - DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=edge
    volumes:
      # Mount for influxdb data directory and configuration
      - ./influxdb/data:/var/lib/influxdb:rw
    ports:
      - 8086:8086
    networks:
      - mqtt

  influxdb2:
    image: influxdb:latest  
    ports:
      - 8087:8086
    environment:
      - DOCKER_INFLUXDB_INIT_MODE=setup 
      - DOCKER_INFLUXDB_INIT_USERNAME=demouser
      - DOCKER_INFLUXDB_INIT_PASSWORD=demouser
      - DOCKER_INFLUXDB_INIT_ORG=influxroadshow 
      - DOCKER_INFLUXDB_INIT_BUCKET=generators
      - DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=edge
    volumes:
      # Mount for influxdb data directory and configuration
      - ./influxdb2/data:/var/lib/influxdb:rw
    networks:
      - mqtt
 
networks: 
  mqtt:
    name: mqtt
